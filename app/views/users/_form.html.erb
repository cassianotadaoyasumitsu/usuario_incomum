<% if current_user.kind == "Usuário" %>
<%= simple_form_for(user) do |f| %>
<%= f.input :prefecture, label: t('prefecture') %>
<%= f.submit user.persisted? ? t('save') : t('create'), class: "btn btn-primary w-100" %>
<% end %>
<% else current_user.kind == "Empreiteira" %>
<%= simple_form_for(user) do |f| %>
<%= f.input :name, label: t('company') %>
<%= f.input :phone, label: t('contact_phone') %>
<%= f.input :address, label: t('address') %>
<%= f.input :note, label: t('about_company') %>
<%= f.submit user.persisted? ? t('save') : t('create'), class: "btn btn-primary w-100" %>
<% end %>
<% end %>
